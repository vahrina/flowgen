name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: publish
        shell: pwsh
        run: dotnet publish .\src\flowgen\flowgen.csproj -c Release -o .\publish

      - name: zip
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path .\package\flowgen -Force | Out-Null
          Copy-Item .\publish\* .\package\flowgen\ -Recurse -Force
          Compress-Archive -Path .\package\flowgen -DestinationPath .\flowgen.zip -Force


      - name: release
        uses: softprops/action-gh-release@v2
        with:
          files: flowgen.zip
